export module ENUMS
{
    export const enum SYNC_MODE
    {
        SYNC_MODE_LOCAL,
        SYNC_MODE_STEPLOCK,
    }
    export const enum INPUT_TYPE
    {
        SCREEN_CLICK,
        TEST_UID
    }
    export const enum RANK_TYPE
    {
        FRIEND_RANK,
        GROUP_RANK
    }
}
